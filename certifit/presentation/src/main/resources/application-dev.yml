spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/certifit}
    username: ${DB_USERNAME:certifit}
    password: ${DB_PASSWORD:certifit}
    driver-class-name: org.postgresql.Driver
